services:
  ollama_agent:
    image: ollama/ollama:latest
    container_name: ${OLLAMA_CNTNAME}
    hostname: ${OLLAMA_HOST}
    ports:
      - "${OLLAMA_EXTERNAL_PORT}:11434"
    environment:
      - OLLAMA_DEBUG=1
    volumes:
      - ${OLLAMA_LOCAL_VOLUME}:/root/.ollama
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 8G
        reservations:
          cpus: '2'
          memory: 4G
          devices:
            - driver: nvidia
              device_ids: ['${OLLAMA_DEVICE_ID}']
              capabilities: [gpu]

  workspace:
    image: m.menschikov/workspace:v10
    container_name: ${WORKSPACE_CNTNAME}
    volumes:
      - ${EXTERNAL_DATA_PATH}:${INTERNAL_DATA_PATH}
      - ${EXTERNAL_NOTEBOOKS_PATH}:${INTERNAL_NOTEBOOKS_PATH}
      - ${EXTERNAL_EXPERIMENTS_PATH}:${INTERNAL_EXPERIMENTS_PATH}
      - ${EXTERNAL_SRC_PATH}:${INTERNAL_SRC_PATH}
      - ${EXTERNAL_MODELS_PATH}:${INTERNAL_MODELS_PATH}
    #depends_on:
    #  - ollama_agent
    deploy:
      resources:
        limits:
          cpus: '8'
          memory: 32G
        reservations:
          cpus: '4'
          memory: 24G
          devices:
            - driver: nvidia
              device_ids: ['${WORKSPACE_DEVICE_ID}']
              capabilities: [gpu]
